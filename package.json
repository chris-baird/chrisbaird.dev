{
    "scripts": {
        "start-dev": "cd server/ && npm start",
        "start": "cd server/ && npm install && node server.js --port=3001",
        "build": "cd client/ && npm install && npm run build"
      }
}